---
interface Props {
    imgSrc: string;
    imgAlt: string;
    classAdded?: string;
}

const { imgSrc, imgAlt, classAdded = "" } = Astro.props;

---

<img src={imgSrc} alt={imgAlt} class={classAdded}>